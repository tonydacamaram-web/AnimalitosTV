<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.AnimalitosTV" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowBackground">@color/fondo_principal</item>
    </style>

    <style name="Theme.AnimalitosTV.Fullscreen" parent="Theme.AnimalitosTV">
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowContentOverlay">@null</item>
    </style>
</resources>
